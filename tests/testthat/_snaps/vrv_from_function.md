# vrv_from_function generates reactives that generate errors as expected

    Code
      signalCondition(error)
    Condition
      Error in `validation_fn()`:
      ! cannot coerce type 'closure' to vector of type 'character'

